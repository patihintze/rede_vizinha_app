import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF205EFF);
  static const Color secondary = Color(0xFF4277FF);
  static const Color background = Colors.white;
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF205EFF);
  static const Color textTerciary = Colors.black;
}